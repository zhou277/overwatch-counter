# Overwatch Counter

当前版本：**v3.1.4**

## v3.1.4 更新

本次优化克制描述的阅读结构：

- 删除大量重复的“英雄核心特点 / 对方打法核心”模板化开头
- 单条克制说明只保留这组对位真正需要注意的内容
- 更强调技能窗口、进场时机、撤退路线、资源交换和集火逻辑
- 英雄自身的通用优势不再反复出现在每一条克制说明中
- 详情底部统一改为 **“英雄特点 / 对局思路”**，集中展示一次英雄的通用打法
- 保留原有数据来源、排名和 evidence 字段

这样阅读时会更接近：

```text
克制关系
→ 为什么这两名英雄在这个对位中存在优势/劣势
→ 应该怎么处理这个具体对位

最后再统一看：
英雄特点 / 对局思路
```

## v3.1.3 更新

本次属于界面样式微调：

- 左侧英雄 / 地图列表宽度从 **330px** 缩窄到 **270px**
- 右侧详情区域获得更多横向空间
- 手机端响应式布局保持不变

## v3.1.2 更新

本次属于界面样式小调整：

- 桌面端页面最大宽度从 **1120px** 增加到 **1440px**
- 左侧英雄 / 地图列表宽度从 **300px** 调整为 **330px**
- 主内容区获得更多横向空间，三列克制卡片阅读更舒适
- 桌面端左右留白减少
- 手机和平板的响应式布局保持不变

## v3.1.1 更新

本次重点更新 **英雄克制数据和克制原因**。

- 依据 2026-09-20 当前版本重新校正克制关系。
- 使用当前 5V5、All Ranks 对位数据作为统计参考。
- 对 D.Va、D.Mon、金驭的 T / C / S Top 3 进行了完整刷新。
- 对全部英雄加入当前版本的主要反制关系锚点，并重新检查排序。
- 954 条克制关系的原因全部改写，不再使用“技能机制、射程或机动性更有优势”这类泛化说明。
- 原因现在尽量明确写出具体技能交互、射程关系、进退场节奏、资源交换、治疗/禁疗、光束/弹道、防守与集火逻辑。
- `counters` Sheet 新增 `source`、`source_date`、`evidence` 三列。
- 新增 `sources` Sheet，记录当前数据来源、时间和用途。
- 修复版本号生成逻辑：`template.html` 改为使用 `__VERSION__` 占位符，之后修改 Excel 的版本号即可正确反映到生成网页。

## 数据来源说明

### Blizzard 官方

用于确认当前英雄改动、技能数值和版本环境。暴雪官方补丁说明并不提供完整的“英雄克制排行榜”。

### CounterWatch

用于当前版本的英雄对位统计参考。其 Counter Rating 关注英雄之间的直接对抗与团战结果，并不等同于整场比赛胜率。

因此本项目的克制关系采用：

```text
当前对位统计
+
英雄技能机制
+
实际阵容与交战逻辑
```

而不是简单把“胜率低”直接解释为“被克制”。

## Excel 数据维护

以后主要修改：

```text
data.xlsx
```

其中：

- `heroes`：英雄名称、T/C/S、搜索别名、英雄说明
- `counters`：克制关系、Top 排名、详细原因、来源与证据
- `maps`：地图推荐
- `tips`：上分建议
- `sources`：数据来源
- `settings`：版本号和当前克制数据版本

## 自动生成

上传新的 `data.xlsx` 到 GitHub 后：

```text
GitHub Actions
→ python build.py
→ 自动生成 index.html
→ 自动提交
→ GitHub Pages 更新
```

通常不需要本地手动运行 Python。

## relation 含义

```text
weak   = 谁克制我
strong = 我克制谁
```

## 位置

```text
T = 坦克
C = 输出
S = 辅助
```

## 版本规则

```text
vX.Y.Z
```

- X：架构更新
- Y：功能更新
- Z：数据调整、Bug 修复、文案或样式小改

本次属于数据校正和构建 Bug 修复：

```text
v3.1.3 → v3.1.4
```
